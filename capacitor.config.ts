import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neurosync.app',
  appName: 'NeuroSync',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
